@model List<Cast>

<link href="/OuterTools/css/bootstrap.min.css" rel="stylesheet">

<section class="panel">
    <header class="panel-heading">
        Yeni Cast eklemek icin <a asp-action="CreateCast">tıklayınız</a>
    </header>
    <div class="panel-body">
        <div class="adv-table">
            <table cellpadding="0" cellspacing="0" border="0" class="display table table-bordered" id="hidden-table-info">
                <thead>
                    <tr>
                        <th>Cast ID</th>
                        <th>Cast FirstName</th>
                        <th>Cast LastName</th>
                        <th class="hidden-phone">Durumu</th>
                        <th class="hidden-phone">Guncelle</th>
                        <th class="hidden-phone">Sil</th>
                        <th class="hidden-phone">Yok et</th>
                    </tr>
                </thead>
                <tbody>

                    @foreach (Cast item in Model)
                    {

                        <tr class="gradeX">
                            <td>@item.ID</td>
                            <td>   @item.FirstName  </td>
                            <td>   @item.LastName  </td>
                            <td class="hidden-phone">@item.Status</td>
                            <td class="center hidden-phone"><a asp-action="UpdateCast" asp-route-id="@item.ID" class="btn btn-warning"> Güncelle </a>   </td>
                            <td class="center hidden-phone">  <a asp-action="DeleteCast" asp-route-id="@item.ID" class="btn btn-danger"> Sil </a>   </td>
                            <td class="center hidden-phone">  <a asp-action="DestroyCast" asp-route-id="@item.ID" class="btn btn-success"> Yok Et </a>   </td>

                        </tr>
                    }


                </tbody>
            </table>

        </div>
    </div>
</section>